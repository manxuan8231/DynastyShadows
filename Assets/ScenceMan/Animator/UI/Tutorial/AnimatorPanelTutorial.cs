using UnityEngine;

public class AnimatorPanelTutorial : MonoBehaviour
{
   public Animator animator;
    void Start()
    {
        animator = GetComponent<Animator>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
